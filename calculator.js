const buttons = document.getElementsByClassName('button-box');

buttons.addEventListener('mouseover', function (e) {
    e.target.style.background = '#6643b5';
});

for (let i = 0; i < array.length; i++) {
    const element = array[i];
    
}